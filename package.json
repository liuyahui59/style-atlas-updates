{
  "name": "style-atlas-updates",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Style Atlas 官方分析规则与软件更新发布仓库。",
  "scripts": {
    "manage": "node scripts/rule-publisher.mjs",
    "check": "node --check scripts/rule-publisher.mjs && node --check src/official-rule-store.mjs && node --check publisher/app.js",
    "test": "node --test tests/official-rule-store.test.mjs",
    "validate": "npm run check && npm test"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
